TO COMPILE access the mains inside PracticeUI and SecretaryUI inside src.ui. Use src and lib folders as build paths to 
build all the resources (e.g. images).

To pay, use find on the secretary UI and enter their name, press appointments then pay. 
